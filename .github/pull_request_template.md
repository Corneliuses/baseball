<!--
Fill in every section below. A PR description that just repeats the diff is not enough —
a reviewer should be able to understand the change and verify it without reading the code first.
-->

## What

<!-- What changed, in plain language. One or two sentences, or a short bullet list for
multi-part changes. Link the issue/ticket this closes, if any. -->

## Why

<!-- Why this change is needed — the problem, bug, or request behind it. Not "what" restated;
the motivation a reviewer can't get from the diff alone. -->

## How to test

<!-- Exact, copy-pasteable steps a reviewer can follow to verify the change themselves.
Replace the placeholders below — do not leave them generic. -->

1. Run this repo's full check gate (see `AGENTS.md` → `## Commands`) — lint, typecheck, tests,
   and build must all pass.
2. <!-- Manual verification: exact command/URL/input to exercise the change, and the expected
   result. E.g. "Run `npm run dev`, visit /projects, confirm the new card renders with a working link." -->
3. <!-- Any edge cases or regressions specifically checked -->
