<!--
Fill in every section below. A PR description that just repeats the diff is not enough —
a reviewer should be able to understand the change, the reasoning behind it, and how to
verify it without reading the code first.
-->

## Summary

<!-- What this PR does and why, in 1-3 sentences. Link the issue it closes, if any. -->

## Key Decisions

<!-- Any non-obvious design or architectural choice this PR makes: the options considered,
which one was chosen, and why — what risk it avoids, or what existing pattern it follows.
Skip this section if the change is small enough that nothing here needs explaining. -->

## What's in Scope

<!-- Bullet list of what this PR implements. -->

### Out of Scope

<!-- What was deliberately left out of this PR, and where it's tracked instead (a follow-up
issue, a later phase). Skip if not applicable. -->

## Bugs Found and Fixed During Self-Review

<!-- If reviewing your own diff before requesting review caught issues, list them: what was
wrong, why it was wrong, and how it was fixed — ideally with a regression test that fails
against the pre-fix code. Delete this section if self-review found nothing to fix. -->

## Known Gaps / Follow-ups

<!-- Anything left unverified, deferred, or out of reach in this environment, and why —
e.g. "can't be verified without a live database", "needs a follow-up issue". Delete if none. -->

## Test Plan

<!-- Exact, copy-pasteable steps a reviewer can follow to verify the change themselves.
Replace the placeholders below — do not leave them generic. -->

- [ ] Run this repo's full check gate (see `AGENTS.md` → `## Commands`) — lint, typecheck,
      tests, and build must all pass.
- [ ] <!-- Manual verification: exact command/URL/input to exercise the change, and the
      expected result. E.g. "Run `npm run dev`, visit /projects, confirm the new card
      renders with a working link." -->
- [ ] <!-- Any edge cases or regressions specifically checked -->
